<div class="page">
	<div class="page_content">
		<div class="page_block">
			<header class="header row g-0">
				<div class="col-3">
					{{> logo}}
				</div>
				<nav class="header_nav col-9 header_nav_right">
					{{!-- {{> nav list=header isVertical=true}} --}}
					{{!-- Шаблонізатор посилається(>) до (nav компонента), який той вже має {{#each list}} ("list"
					(можна
					будь-яке
					слово)), а вже тут в контейнері шаблонізатор каже що (list дорівнює header), тобто header з
					енд-поінта!
					--}}
					<div class=".header_nav_right">
						{{> nav list=header}}
					</div>
				</nav>
			</header>
			{{> divider}}
		</div>

		{{!-- {{> hero data=hero}}
		викликаємо ( >hero) і вказуєм що (дані (data)) будуть приходити в змінній (hero що знаходиться в
		енд-поінті) --}}
		<div class="page_block">
			{{> hero title=hero.title text=hero.text}}
		</div>
		{{!-- title і text, що знаходяться в компоненті "hero" дорівнюють шляху в енд-поінті (hero.title)(hero.text)
		--}}

		<div class="page_block">
			<main class="row g-0 plans">
				{{!-- все що ми ітеруємо в plans, виводить саме {{this}}, попередньо data вказує на this, а this вже
				звертається до усіх об'єктів даних--}}
				{{#each plans}}
				<div class="col plans_card">
					{{> card data=this}}
				</div>
				{{/each}}
			</main>
		</div>

		<div class="page_block">
			{{> divider}}
			<footer class="row footer">
				<div class="col-3">
					{{> logo}}
					<span class="footer_copyright">Copyright 2023-2024</span>
				</div>

				{{#each footer}}
				<div class="col-3">
					<span class="footer_title">{{title}}</span>
					{{> nav list=links isVertical=true}}
				</div>
				{{/each}}

			</footer>
		</div>
	</div>
</div>